# إصلاح زر تحديد الموقع — 7 سبتمبر 2026

السبب: زر خريطة العميل كان ينفذ controller.move(center, 13)، وزر المتجر يعيد التكبير إلى selected. لم توجد قراءة لموقع الجهاز أو أذونات Android.

الإصلاح:
- إضافة geolocator وأذونات الموقع التقريبي والدقيق أثناء استخدام التطبيق فقط.
- خدمة مشتركة لفحص تشغيل الموقع وطلب الإذن وجلب إحداثيات حديثة بمهلة 25 ثانية. لا تُستعمل نقطة مدينة ثابتة أو موقع مخزن بديلًا عن نتيجة الهاتف.
- زر مشترك يمنع تكرار الطلب، يعرض التقدم والأخطاء بالعربية، ويوفر فتح الإعدادات عند الحاجة. يهمل النتيجة إذا أُغلقت الشاشة.
- خريطة العميل تتحرك إلى موقع الهاتف وتعرض علامة عليه. خريطة المتجر تحدث الموقع المختار، مع استمرار إمكانية الاختيار باللمس.
- أثناء تسجيل متجر جديد يعود اختيار الإحداثيات إلى نموذج التسجيل دون كتابة سجل إداري. حفظ موقع متجر قائم بقي على مساره الحالي مع عرض فشل الحفظ للمستخدم.
- تحديث مسودة الخصوصية بوصف استخدام الموقع.

التحقق: نجحت مجموعة Flutter ذات 55 اختبارًا ثم اختبارا تكامل الخريطتين (57 إجمالًا). تستخدم الاختبارات إحداثيات بديلة ولا تمثل اختبار GPS أو الأذونات على هاتف فعلي.

للتجربة: تثبيت APK الجديد، تشغيل الموقع في الهاتف، ثم الضغط على «تحديد موقعي» والسماح بالموقع أثناء الاستخدام. عند الرفض الدائم يُستخدم زر الإعدادات الظاهر في الرسالة. تحتاج صور الخريطة اتصالًا بالإنترنت؛ تحميلها مستقل عن قراءة إحداثيات الهاتف.

هذا إصلاح Android في المشروع الحالي. قبل بناء iOS لاحقًا، يجب إضافة NSLocationWhenInUseUsageDescription وإعدادات geolocator الخاصة بـApple؛ مجلد ios غير موجود حاليًا.

سجل بناء النسخة: artifacts/location-fix-android-build.log.
اكتمل assembleDebug خلال 384.9 ثانية، ونسخة التسليم في artifacts/azharna-location-fix-debug.apk. فُحصت أذونات الموقع من APK باستخدام aapt. أعاد غلاف PowerShell رمزًا غير صفري بسبب تحذيرات stderr، بينما سجل Gradle يؤكد إنشاء الحزمة الجديدة بنجاح. التحليل النهائي بلا ملاحظات. لم تثبّت النسخة على هاتف خلال هذا العمل.
مرجع المكتبة: https://pub.dev/packages/geolocator
