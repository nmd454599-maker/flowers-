# تنفيذ مركز السوبر أدمن

آخر تحديث: 2026-09-02

## المنفذ محليًا

- تفعيل الأقسام الـ12 داخل `AdminToolsScreen`.
- حفظ إعدادات وإجراءات كل قسم داخل `adminData/{scope}/records`.
- قصر القراءة والكتابة على حساب إداري نشط عبر قواعد Firestore.
- تسجيل كل تغيير داخل `auditLogs` مع المنفذ والهدف والتفاصيل.
- تحميل مؤشرات المستخدمين والمتاجر والطلبات والمبيعات من Firestore.
- تسجيل أجهزة المستخدمين في مواضيع FCM وحفظ رمز الجهاز.
- تجهيز Cloud Functions في منطقة `me-central1` لمعالجة:
  - الإشعارات الفورية والمجدولة.
  - إنهاء جلسات تسجيل الدخول.
  - حظر الحسابات.
  - إنشاء قسائم التعويض.
  - إعداد ملخصات التقارير المالية والتحليلية.
- إضافة اختبارات لحفظ إعدادات الإدارة وظهور الأدوات الـ12.

## حالة التحقق

- `flutter analyze`: ناجح بلا ملاحظات.
- `flutter test`: نجحت 9 اختبارات.
- فحص صياغة Cloud Functions: ناجح.
- بناء APK بوضع Firebase: ناجح.

## المطلوب لتفعيل الإنتاج

1. ~~نشر قواعد Firestore الجديدة~~ — تم بنجاح في 2026-09-02.
2. ترقية المشروع إلى Blaze لإنشاء Storage ونشر Cloud Functions.
3. إنشاء Storage في `me-central1` ثم نشر `storage.rules`.
4. نشر Functions ثم إجراء اختبار قبول بحساب `superAdmin` حقيقي.

## ملاحظة أمنية

لا يستطيع التطبيق منح صلاحية `superAdmin` لحساب جديد. يجب إنشاء أول حساب إداري
موثوق يدويًا، وبعده تُدار الحسابات وفق القواعد وسجل التدقيق.
